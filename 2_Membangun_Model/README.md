# Kriteria 2 - Membangun Model dengan MLflow

- `modelling.py`: training basic dengan MLflow autolog.
- `modelling_tuning.py`: training advanced dengan hyperparameter tuning dan manual logging ke MLflow/DagsHub.

Jalankan:

```bash
pip install -r requirements.txt
python modelling_tuning.py
```
