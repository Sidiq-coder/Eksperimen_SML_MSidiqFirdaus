name: titanic-mlflow-env

channels:
  - conda-forge
  - defaults

dependencies:
  - python=3.11
  - pip
  - pip:
      - pandas
      - numpy
      - scikit-learn
      - mlflow
      - matplotlib
      - joblib