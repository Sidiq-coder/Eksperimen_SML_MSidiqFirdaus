name: sml-titanic-env

channels:
  - conda-forge
  - defaults

dependencies:
  - python=3.10
  - pip
  - pip:
      - pandas
      - numpy
      - scikit-learn
      - mlflow
      - matplotlib
      - seaborn
      - joblib
